services:
  gatus:
    image: twinproduction/gatus:latest
    container_name: nitech-status
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      GATUS_CONFIG_PATH: /config
    volumes:
      - ./config:/config:ro
      - gatus-data:/data

volumes:
  gatus-data:
